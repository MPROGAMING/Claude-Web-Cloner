// SheetHeader only renders inside Sheet, so these cells are the real Sheet
// compositions, named for what SheetHeader contributes to each.
export { ProjectSettings as TitleAndDescription, FromTheBottom as ShortHeader } from "./Sheet";
