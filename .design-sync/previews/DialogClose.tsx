// DialogClose only renders inside Dialog, so these cells are the real Dialog
// compositions, named for what DialogClose contributes to each.
export { ApplyChangeSet as CornerCloseButton, RenameProject as WithFooterCancel } from "./Dialog";
