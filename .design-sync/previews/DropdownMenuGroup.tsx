// DropdownMenuGroup only renders inside DropdownMenu, so these cells are the real DropdownMenu
// compositions, named for what DropdownMenuGroup contributes to each.
export { GroupedSections as TwoLabelledGroups } from "./DropdownMenu";
