// DropdownMenuShortcut only renders inside DropdownMenu, so these cells are the real DropdownMenu
// compositions, named for what DropdownMenuShortcut contributes to each.
export { ProjectActions as TrailingKeyHint } from "./DropdownMenu";
