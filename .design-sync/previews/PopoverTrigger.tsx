// PopoverTrigger only renders inside Popover, so these cells are the real Popover
// compositions, named for what PopoverTrigger contributes to each.
export { StudioConnection as OutlineTrigger, CreditsBalance as GhostTrigger } from "./Popover";
