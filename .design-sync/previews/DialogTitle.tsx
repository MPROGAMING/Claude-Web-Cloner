// DialogTitle only renders inside Dialog, so these cells are the real Dialog
// compositions, named for what DialogTitle contributes to each.
export { DeleteProject as DestructiveQuestion, NewProject as PlainTitle } from "./Dialog";
