// DropdownMenuTrigger only renders inside DropdownMenu, so these cells are the real DropdownMenu
// compositions, named for what DropdownMenuTrigger contributes to each.
export { ProjectActions as IconButtonTrigger, GroupedSections as LabelledTrigger } from "./DropdownMenu";
