// PopoverContent only renders inside Popover, so these cells are the real Popover
// compositions, named for what PopoverContent contributes to each.
export { StudioConnection as FullPopover, PlainNote as DescriptionOnly } from "./Popover";
