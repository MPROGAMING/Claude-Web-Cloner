// SheetFooter only renders inside Sheet, so these cells are the real Sheet
// compositions, named for what SheetFooter contributes to each.
export { ProjectSettings as SaveAndCancel, FromTheBottom as TwoChoices } from "./Sheet";
