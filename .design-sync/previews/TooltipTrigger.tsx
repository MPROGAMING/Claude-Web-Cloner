// TooltipTrigger only renders inside Tooltip, so these cells are the real Tooltip
// compositions, named for what TooltipTrigger contributes to each.
export { OnAnIconButton as IconButton, OnADestructiveAction as DestructiveIconButton } from "./Tooltip";
