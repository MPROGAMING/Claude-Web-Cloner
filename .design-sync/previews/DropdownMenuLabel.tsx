// DropdownMenuLabel only renders inside DropdownMenu, so these cells are the real DropdownMenu
// compositions, named for what DropdownMenuLabel contributes to each.
export { GroupedSections as SectionHeadings } from "./DropdownMenu";
