// SheetContent only renders inside Sheet, so these cells are the real Sheet
// compositions, named for what SheetContent contributes to each.
export { ProjectSettings as FromTheRight, FromTheLeft as FromTheLeft, FromTheBottom as FromTheBottom } from "./Sheet";
