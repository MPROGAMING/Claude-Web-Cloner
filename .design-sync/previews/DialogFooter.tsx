// DialogFooter's only true render is inside a Dialog, so its cells are the real
// dialog compositions, named for the axis the footer actually varies on.
export { ApplyChangeSet as ConfirmAndCancel, DeleteProject as DestructiveAction } from "./Dialog";
