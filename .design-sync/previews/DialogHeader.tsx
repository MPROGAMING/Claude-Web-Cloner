// DialogHeader only renders inside Dialog, so these cells are the real Dialog
// compositions, named for what DialogHeader contributes to each.
export { ApplyChangeSet as TitleAndDescription, RenameProject as ShortHeader } from "./Dialog";
