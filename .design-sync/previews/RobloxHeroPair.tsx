import { RobloxHeroPair } from "blockwright";

// Two real Roblox places above the fold, in two genres — a sconce-lit horror
// corridor and a daylit island scene. Both captions and both crops live in the
// component; className is the only prop. The photographs are served from the
// app's /demos directory, so they are absent from a preview bundle.
export const TwoGenres = () => <RobloxHeroPair />;
