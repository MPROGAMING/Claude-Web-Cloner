// TooltipProvider only renders inside Tooltip, so these cells are the real Tooltip
// compositions, named for what TooltipProvider contributes to each.
export { Sides as WrapsEveryTooltip } from "./Tooltip";
