// ProgressValue only renders inside Progress, so these cells are the real Progress
// compositions, named for what ProgressValue contributes to each.
export { WithLabel as PercentReadout, Sweep as AcrossTheRange } from "./Progress";
