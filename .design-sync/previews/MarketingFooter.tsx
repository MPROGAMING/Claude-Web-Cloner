import { MarketingFooter } from "blockwright";

// Takes no props: the link columns, the Roblox trademark disclaimer and the
// Game-icons attribution are all part of the component. Rendered on every
// marketing route by src/app/(marketing)/layout.tsx.
export const Default = () => <MarketingFooter />;
