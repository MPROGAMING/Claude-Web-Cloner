// DialogDescription only renders inside Dialog, so these cells are the real Dialog
// compositions, named for what DialogDescription contributes to each.
export { ApplyChangeSet as ExplainsTheAction, DeleteProject as WarnsAboutLoss } from "./Dialog";
