// DropdownMenuSubTrigger only renders inside DropdownMenu, so these cells are the real DropdownMenu
// compositions, named for what DropdownMenuSubTrigger contributes to each.
export { WithSubmenu as OpensOnTheRow } from "./DropdownMenu";
