import { DemoShowcase } from "blockwright";

// A whole marketing band: its own eyebrow, headline, the two Studio captures
// (a player-height bridge shot and a close prop detail) and the note about what
// was generated versus built. Self-contained apart from className — the
// screenshots come from the app's /demos directory.
export const Default = () => <DemoShowcase />;
