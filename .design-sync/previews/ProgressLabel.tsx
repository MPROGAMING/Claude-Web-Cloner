// ProgressLabel only renders inside Progress, so these cells are the real Progress
// compositions, named for what ProgressLabel contributes to each.
export { WithLabel as LabelledBar, InACreditsCard as InContext } from "./Progress";
