// DialogContent only renders inside Dialog, so these cells are the real Dialog
// compositions, named for what DialogContent contributes to each.
export { ApplyChangeSet as DefaultWidth, NewProject as WideWidth } from "./Dialog";
