// SheetClose only renders inside Sheet, so these cells are the real Sheet
// compositions, named for what SheetClose contributes to each.
export { ProjectSettings as CornerAndFooterClose } from "./Sheet";
