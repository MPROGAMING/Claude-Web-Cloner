// SheetTitle only renders inside Sheet, so these cells are the real Sheet
// compositions, named for what SheetTitle contributes to each.
export { ProjectSettings as SettingsTitle, FromTheBottom as AlertTitle } from "./Sheet";
