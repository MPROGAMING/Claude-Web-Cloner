// DropdownMenuContent only renders inside DropdownMenu, so these cells are the real DropdownMenu
// compositions, named for what DropdownMenuContent contributes to each.
export { ProjectActions as OpenMenu, GroupedSections as GroupedMenu } from "./DropdownMenu";
