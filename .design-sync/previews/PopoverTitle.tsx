// PopoverTitle only renders inside Popover, so these cells are the real Popover
// compositions, named for what PopoverTitle contributes to each.
export { StudioConnection as ConnectionTitle, CreditsBalance as BalanceTitle } from "./Popover";
