// PopoverHeader only renders inside Popover, so these cells are the real Popover
// compositions, named for what PopoverHeader contributes to each.
export { StudioConnection as TitleAndDescription, CreditsBalance as BalanceHeader } from "./Popover";
