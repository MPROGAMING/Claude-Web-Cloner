// DialogOverlay only renders inside Dialog, so these cells are the real Dialog
// compositions, named for what DialogOverlay contributes to each.
export { DeleteProject as DimmedBackdrop, NewProject as OverWiderContent } from "./Dialog";
