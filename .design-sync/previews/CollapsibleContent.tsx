// CollapsibleContent only renders inside Collapsible, so these cells are the real Collapsible
// compositions, named for what CollapsibleContent contributes to each.
export { ToolCallExpanded as RevealedJson, AStackOfCalls as OneOpenInAStack } from "./Collapsible";
