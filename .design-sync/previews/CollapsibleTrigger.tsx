// CollapsibleTrigger only renders inside Collapsible, so these cells are the real Collapsible
// compositions, named for what CollapsibleTrigger contributes to each.
export { ToolCallExpanded as ExpandedRow, Collapsed as CollapsedRow } from "./Collapsible";
