// SheetTrigger only renders inside Sheet, so these cells are the real Sheet
// compositions, named for what SheetTrigger contributes to each.
export { ProjectSettings as ButtonTrigger, FromTheLeft as GhostTrigger } from "./Sheet";
