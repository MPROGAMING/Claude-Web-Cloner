// PopoverDescription only renders inside Popover, so these cells are the real Popover
// compositions, named for what PopoverDescription contributes to each.
export { PlainNote as StandsAlone, StudioConnection as UnderATitle } from "./Popover";
